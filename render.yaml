services:
  - type: web
    name: detector-mascarilla
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn "Detector con Flask":app
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
